<h1><%= @question.title %></h1>
<%= @question.body %>
<h1><%= link_to 'Back', root_path %></h1>
<h2>Answers</h2>
<%= render @answers %>

<%= form_with model: [@question, @new_answer] do |form| %>
  <%= form.rich_text_area :body, class: 'trix-content' %>
  <%= form.submit 'Post Answer' %>
<% end %>
